FOLLOW-UP TASK (your worktree, branch research/fidelity-langbugs). Your language-tag work continues; the owner has now made three decisions that expand your scope. Commit incrementally; deliverable REPORT2.md. Main repo remains read-only reference; the auditor file tools/verify_atlas_source_fidelity.py is owned by another line of work — do NOT edit it (you may read it and run it).

OWNER DECISION 1 — ENGLISH-ONLY IS DECLARED, NOT FIXED. Atlas's scope is English content; publishers' 40+ other languages are deliberately not carried (97.68% of all audit differences). Your job on this: make the scope EXPLICIT in the product, not just true in practice.
  a. The builder/adapters: the English selection currently happens implicitly in several places (_english() in src/refspec/atlas/v3_registry_vocabularies.py:187 and adapter-local tests). Consolidate to ONE documented predicate — and per decision 2 below it must be BCP-47 family-aware (en, en-US, en-GB...), not exact-match.
  b. Record the scope where consumers read it: bindings/atlas/3.1/README.md gets a short normative paragraph ("Atlas carries English-language content; publisher content in other languages is deliberately not represented"), and the distribution's construction summary or producer validation gains a machine-readable statement of the language scope (pick the honest place — it is a property of the BUILD, not of the contract; wire-visible, so state the field and its schema change in your report).
  c. Do NOT write the auditor-side declaration (another line owns that file). Instead specify EXACTLY what it needs: per-source, per-language claim counts and the predicate families involved, in a form the DeclaredClaimExclusion mechanism can consume. Put that spec in REPORT2.md.

OWNER DECISION 2 — CARRY ENGLISH DEFINITIONS AND SCOPE NOTES. Measured by the parallel research (branch research/fidelity-definitions, its REPORT.md and prototype-measurements.json — read them): +1,885 quads, ~82KB compressed, and the wire/compact/Parquet/search-view fields ALREADY EXIST. Implement it: adapters populate atlas:definition/atlas:note (check exact predicate names in bindings/atlas/3.1/ontology/atlas.ttl) for English skos:definition and skos:scopeNote from the eight vocabulary sources that ship them (EuroVoc is ~all of it). Mind: definitions enter node digests — measure and report the re-mint radius; the language predicate from decision 1 governs which claims qualify; skos:historyNote is OUT of scope for this change (it is neither definition nor scope note — say so in the report).

OWNER DECISION 3 (context only, no work): move 2 proceeds; unrelated to you.

VERIFY: bounded builds only (--only-release, seconds each) for the affected sources; run the auditor SCOPED (--only <spec>) against your rebuilt bounded artifacts to prove the definitions now flow and the 244 en-US labels appear; make lint; the adapter tests; make contract-dev if your changes touch anything it covers. NEVER the ~25-min full build.

REPORT2.md: the consolidated language predicate and every call site it replaced; the language-scope statement's location and schema delta; the definitions carry with per-source claim counts, measured wire growth, and re-mint radius; the auditor-declaration spec (decision 1c); before/after scoped audit output; what a main-tree merge needs.
