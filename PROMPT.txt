You are a product-research engineer in a DISPOSABLE git worktree of an RDF vocabulary pipeline. Deliverable: the decision package for a real product gap, as REPORT.md + prototype measurements, committed to branch research/fidelity-definitions. Do NOT touch the main repo at /Users/mikewolfd/Work/spicy-regs/RefSpec outside read-only references to its output/ artifacts.

THE GAP (measured by the fidelity audit): EuroVoc's publisher file carries 43,837 definitions and scope notes; the Atlas distribution carries ZERO of them. This is the largest single verified content omission in the product. The owner must decide: CARRY them (builder change, wire growth) or DECLARE them out of scope (auditor declaration, honest omission). Your job is to price both sides so the decision is arithmetic.

TASKS:
1. SURVEY the full landscape, not just EuroVoc: for each of the 8 bulk-SKOS sources the audit covers (eurovoc, elsst, gemet, agrovoc, nasa-thesaurus, osti, eurovoc-domains, nalt - find the exact set in the auditor's SOURCES), count publisher-side definitions/scopeNotes/notes/examples (skos:definition, skos:scopeNote, skos:note, skos:example, skos:historyNote, skos:editorialNote + rdfs:comment) vs what Atlas carries (the wire has atlas:definition/atlas:note - grep the ontology bindings/atlas/3.1/ontology/atlas.ttl and check real packs). Produce the per-source omission table. Publisher sources: /Users/mikewolfd/Work/spicy-regs/RefSpec/output/registry-real-data-sources/ (read-only).
2. PRICE the CARRY side: read the builder's label/annotation handling (tools/generate_atlas_v3_full.py + the adapters in src/refspec/registry/) - what changes to carry English definitions/scopeNotes for these sources; estimate wire growth (bytes, quads - measure actual definition text volume from publisher files); note the SHACL/schema surface touched (the wire already HAS atlas:definition/atlas:note for some sources - which adapters populate it today and which do not? That asymmetry IS the finding); estimate re-mint blast radius (do definitions enter node digests / identity? check rdf_node_digest facts).
3. PRICE the DECLARE side: what the auditor's DeclaredClaimExclusion mechanism would record (per-source, per-predicate claim counts), and the product honesty cost - the README/comparison docs claim the product's differentiator is fidelity; quantify what percent of publisher semantic content (by triple count) the product would be declaring away.
4. SEARCH-VALUE ANGLE: definitions are prime search-lane content (SpicySearch's semantic lane embeds segments; concept definitions could strengthen the concept lane). One paragraph on what carrying definitions would enable downstream, grounded in how the search view is built (src/refspec/atlas/parquet_search_view.py - what would a definitions column cost there).
5. REPORT.md: the omission table, both prices with measured numbers, the asymmetry finding (which adapters already carry notes), and a recommendation framed as advice - the owner decides.

Read-only on all artifacts; prototype measurements in the worktree. Peak RSS under 6GB.
