You are a performance researcher in a DISPOSABLE git worktree of an RDF validation codebase (Python, rdflib 7.5, pySHACL 0.31). Deliverable: REPORT.md in this worktree root + prototypes + measurements, committed to branch research/residual-shacl as you go. Do NOT touch the main repo at /Users/mikewolfd/Work/spicy-regs/RefSpec outside read-only references to its output/ artifacts.

MISSION: after the warrant-xone lift, the batched pySHACL fast path still costs ~430s (~10 min projected) at full scale on a GREEN graph - the residual engine share of acceptance. Find what it is spending that on and how much is removable while pySHACL stays the engine and every verdict + constraint-component output stays identical.

Read first: plans/validation-cost-reset-plan.md "Inside-the-phases trace" (the xone lift precedent: parse the constraint off the shapes graph, evaluate via table lookups, refuse-and-fall-back on shape drift, corpus-proven equivalence - measured 95.8x on its constraint) and kill-list item 5. The code: bindings/atlas/3.1/tools/validate.py - _batched_shacl_plan, _batched_shacl_precheck_misses, the lifted closed-shape and ring-context prechecks, _run_shacl, _validate_shacl_data. The shapes: bindings/atlas/3.1/shapes/atlas.shacl.ttl (~37 node shapes, 260+ property shapes).

METHOD:
1. Profile the batched engine at staging scale (artifact below): which shapes and which constraint components dominate the remaining 14-18s? cProfile + pySHACL-internal attribution.
2. For the top cost centers, evaluate lifting MORE constraints into table-driven prechecks using the existing lift pattern (sh:hasValue/sh:in/sh:minCount/sh:maxCount/sh:datatype/sh:nodeKind on direct paths are all mechanically liftable; sh:class needs the type index the parse observer already builds). Prototype the biggest 2-3 lifts, measure, and prove corpus equivalence: the corpus is bindings/atlas/3.1/fixtures/ - regenerate in YOUR worktree if absent (tools/build_fixtures.py) and run the full validate_binding gate (make test-atlas-v3 equivalent from your worktree); all cases must keep their firstIssue codes and shaclComponents lists.
3. Consider engine-level wins that change no semantics: shape-graph parse caching, pySHACL invocation overheads, the per-focus-node dispatch cost visible in prior samples (list_contains over CONSTRAINT_PARAMETERS, Node.__eq__ churn).
4. Report the honest floor: what remains irreducible while pySHACL is the engine, and at what point the lift pattern has effectively REPLACED the engine on the green path (an interesting finding in itself - if 90%+ of green-path constraints are table-evaluated, say so plainly).

ARTIFACTS: staging distribution /Users/mikewolfd/Work/spicy-regs/RefSpec/output/atlas-3.1-mapping-topology-staging/distribution (1,013,723 quads) - READ ONLY. Peak RSS under 8GB. REPORT.md: measured per-avenue numbers, full-scale projections with stated assumptions, corpus-equivalence proof output, implementation cost, TAKE/DONT-TAKE against the once-per-release bar.
