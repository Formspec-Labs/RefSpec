You are a performance researcher in a DISPOSABLE git worktree of an RDF validation codebase (Python, rdflib 7.5, pySHACL 0.31). Your deliverable is REPORT.md in this worktree root, plus prototype code + measurements. Commit your work to the current branch (research/parse-substrate) as you go. Do NOT touch the main repo at /Users/mikewolfd/Work/spicy-regs/RefSpec outside read-only references to its output/ artifacts.

MISSION: the acceptance validator's rdflib parse+store phase costs ~1,373s (17 min) at full scale (29.3M quads, ~20GB RSS) and is the largest residual cost after recent optimizations. Investigate cutting it WITHOUT changing the SHACL engine (pySHACL requires an rdflib Graph API) and report what is achievable.

Read first: plans/validation-cost-reset-plan.md (the "Inside-the-phases trace" section and the oxigraph-verdict paragraph in the findings register - prior measurements: rdflib stock parse 12.6s/1M quads staging, oxigraph 1.7s, the oxrdflib shim 4.2x slower AND lossy so it is ruled out). The validator: bindings/atlas/3.1/tools/validate.py - _parse_packed_dataset/_parse_pack_into_dataset/_LexicalNQuadsParser, the _AssertedPlacementObservation observer pattern, and what downstream checks actually READ from the graph (SemanticInventory usage, targeted triples() calls, the SHACL validation view _ShaclDataView).

AVENUES to evaluate (measure, don't speculate):
1. A custom rdflib Store: rdflib Memory builds three full triple indexes (spo/pos/osp) with per-triple context bookkeeping. Profile which indexes the residual validator + pySHACL actually query at staging scale; prototype a leaner Store subclass (fewer indexes, interned terms, slotted classes, arrays instead of nested dicts) that still satisfies the Graph API pySHACL touches; measure parse+store and end-to-end validation time + RSS vs Memory.
2. Term interning / object churn: prior profiling showed Node.__eq__ 120M calls and term __new__ ~16M. Measure whether sys.intern on lexical forms + __slots__ + cached hash on a Literal/URIRef subclass moves the needle.
3. Parallel pack parsing into shards: packs are independent sorted files; rdflib stores are not thread-safe but multiprocess parse + merge (or per-shard graphs unioned via a read-only aggregate like the existing _ShaclDataView) may divide wall time - the plan warns naive multiprocess multiplies the 18GB; evaluate a bounded-worker design with per-worker RSS measured.
4. Anything better you find profiling - follow the data.

ARTIFACTS: staging distribution (1,013,723 quads): /Users/mikewolfd/Work/spicy-regs/RefSpec/output/atlas-3.1-mapping-topology-staging/distribution - READ ONLY. Use it for all measurements; extrapolate honestly (prior work found rdflib 1.5x superlinear at 22GB heap - state your extrapolation assumptions). Keep peak RSS under 8GB (other work shares this machine).

REPORT.md must carry: per-avenue measured numbers (staging) + honest full-scale projection, the winning design's compatibility proof with pySHACL (run the real shapes: bindings/atlas/3.1/shapes/atlas.shacl.ttl against your store, verdicts must match rdflib Memory exactly), implementation cost estimate, and a clear TAKE/DONT-TAKE recommendation against the bar: this runs once per release, unattended.
