You are a contract engineer in a DISPOSABLE git worktree. Deliverable: the move-2 implementation path, prototyped and measured, committed to branch research/move2-compiler, plus REPORT.md. The main repo at /Users/mikewolfd/Work/spicy-regs/RefSpec is READ-ONLY reference. The upstream repo ~/Work/rulespec is where the compiler lives - you MAY work there ONLY in a worktree you create yourself (git -C ~/Work/rulespec worktree add ...); never edit ~/Work/rulespec's main checkout.

OWNER DECISION: pursue boundary-collapse move 2 - "the CUE compiler emits the check; consumers execute, never re-implement". The correctness half is already proven: branch research/shacl-sparql (worktree .claude/worktrees/codex-shacl-sparql/) carries a working SHACL-SPARQL prototype of four rkaf adjudication rules with 26/26 fixture checks agreeing across pySHACL and Jena. READ ITS REPORT.md FIRST - especially its corrections: the deletion is ~128 Python lines not 900, the prototype is 164 lines (code-neutral), pySHACL runs the four rules in 3.24 min at bounded staging scale (viable once-per-release), and the staging artifact contains ZERO adjudication records so its timings used a derived target-loaded view.

Context in the main repo (read-only): plans/validation-cost-reset-plan.md's "The rulespec boundary, collapsed" section (move 2's statement + the guardrails: rulespec stays generic, no Atlas-specific terms upstream), docs/decisions.md REF-026/027/028/029, AGENTS.md's differential-oracle policy (any check replacement keeps the old implementation as a test-only oracle and proves agreement over real data + a mutation battery BEFORE deletion - this is binding on move 2), and bindings/atlas/3.1/tools/validate.py's _check_machine_adjudication block (~validate.py:4715-5230).

TASKS:
1. THE COMPILER SIDE (in your rulespec worktree): rulespec authors constraints in CUE (constraints/analysis/*.cue - find the adjudication ones: resolver-proof-record.cue, machine-adjudication.cue) and compiles to JSON Schema + SHACL (tools/constraints_compile.py, compile_all.sh). Determine what it would take to EMIT the four sh:sparql shapes from the CUE source of truth rather than hand-writing them. Options to evaluate honestly: (a) full generation from CUE structure, (b) CUE carries the SPARQL text as annotated strings the compiler passes through with the shape scaffolding generated, (c) a hybrid where mechanical parts generate and genuinely-relational rules are authored beside the CUE with a currency test. Prototype whichever is viable; if (a) is not viable for relational rules, say so with the reason - that is a finding, not a failure.
2. THE PACKAGING: the compiled shapes must reach RefSpec through the existing rulespec-conformance wheel (src/rulespec_conformance/contract/ - the package already ships compiled SHACL and a resources accessor). Wire the emitted shapes into that package's data + accessors; prove they load from an installed wheel in a scratch venv (the package's own precedent: make test-package).
3. THE CONSUMER SIDE (prototype only, in YOUR worktree copy of the RefSpec tree - your worktree IS a RefSpec checkout): show how validate.py would consume the upstream shapes: the binding's shapes graph gains the upstream sh:sparql shapes (from the package, digest-pinned), and the ~128 lines of Python adjudication logic those four rules replace become... what exactly? Note the constraint: the binding validator deliberately imports NO RefSpec package code (it must stay copy-and-run). So the upstream shapes must arrive as DATA in the binding directory, refreshed by a generator with a currency check - design that, and state where the digest pin lives.
4. THE ORACLE OBLIGATION: per AGENTS.md, prototype the differential harness: old Python check vs new sh:sparql shape, agreeing over the corpus AND a mutation battery. The shacl-sparql branch has the fixture-scale equivalence; you need the mutation dimension and a statement of what "real data" means here (the staging artifact has no adjudication records - name what artifact would).
5. REPORT.md: the emission design with its verdict on (a)/(b)/(c), the packaging proof, the consumer-side design with the independence constraint respected, the oracle harness, the honest cost (weeks? days?), what the ~128-line deletion actually buys (single-source generation, drift-class elimination), and what blocks production adoption today.

Peak RSS under 6GB. Bounded/fixture-scale runs only; never the ~25-min full build.
