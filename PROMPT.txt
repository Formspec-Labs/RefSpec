You are a data-fidelity engineer in a DISPOSABLE git worktree of an RDF vocabulary pipeline. Deliverable: SourceSpec coverage expansion for the fidelity auditor, committed incrementally to branch research/fidelity-coverage, plus REPORT.md. Do NOT touch the main repo at /Users/mikewolfd/Work/spicy-regs/RefSpec outside read-only references to its output/ artifacts. Note: the main tree's copy of the auditor is being edited concurrently - your branch will be merged later, so keep your changes ADDITIVE where possible (new specs, new reader kinds) and localized.

CONTEXT: tools/verify_atlas_source_fidelity.py audits an Atlas distribution against pinned publisher source bytes. Current coverage: 24 of 110 construction units have a declared SourceSpec comparison; 86 are UNCOVERED (explicit failures). The receipt at /Users/mikewolfd/Work/spicy-regs/RefSpec/output/atlas-3.1-full-2026-08-12c-source-fidelity-receipt-batch1.json (613KB, readable) lists every unit and its status. The campaign goal is 24 -> 110.

TASKS:
1. INVENTORY the 86 uncovered units: read the receipt's distribution-coverage section + the registry modules (src/refspec/registry/) that construct each unit. Classify by source kind: bulk SKOS (rdf/ttl), JSON API captures, CSV/XLSX, PDF extracts, native-control Parquet. Rank by (records represented x authoring cost).
2. AUTHOR SourceSpecs for the cheapest, largest wins first. The existing 24 specs (grep SOURCES/SourceSpec in the auditor) are your pattern library; the FR source-extract kind (check_source_extract_fidelity) shows how a checked-extract comparison works. Bulk SKOS sources whose reader kinds already exist should be near-pure declarations. For kinds with no reader (JSON API shapes, CSV), implement the narrowest honest stock-parser reader (json/csv stdlib, no ETL imports) per the auditor's independence rule.
3. VERIFY each batch of specs with the auditor's --only flag against the real distribution: uv run --no-sync python tools/verify_atlas_source_fidelity.py --distribution /Users/mikewolfd/Work/spicy-regs/RefSpec/output/atlas-3.1-full-2026-08-12c --source-root /Users/mikewolfd/Work/spicy-regs/RefSpec/output/registry-real-data-sources --only <spec> --output <scratch>. A new spec that reports differences is FINE (honest coverage); a spec that errors or reports nonsense is not. Keep runs scoped (--only) - never the full audit repeatedly.
4. Add tests following tests/test_verify_atlas_source_fidelity.py patterns for each new reader kind (not each spec): faithful-pair passes, one fault injection each.
5. REPORT.md: the inventory table (unit, kind, records, spec status), coverage before/after (24 -> N), per-spec verification results, what remains uncovered and why (missing pinned sources? unparseable kinds?), estimated cost to finish the campaign.

Work in payoff order; commit after each verified batch so partial progress survives. Peak RSS under 6GB; other work shares this machine.
