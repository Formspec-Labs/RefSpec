You are a fidelity-auditor engineer in a DISPOSABLE git worktree (branch research/auditor-language-scope). Deliverable: two auditor changes, implemented and verified, plus REPORT.md, committed to this branch. The main repo /Users/mikewolfd/Work/spicy-regs/RefSpec is READ-ONLY reference (read its output/ artifacts; never write there). You own tools/verify_atlas_source_fidelity.py and tests/test_verify_atlas_source_fidelity.py. Four other agents hold that same file in other worktrees for SourceSpec additions - keep your changes to the MECHANISM localized so the merges do not fight.

TASK 1 - THE FREE WIN (do it first, it is the smallest and most certain).
The auditor double-counts NASA annotations: zthes:termNote values are note-KIND markers (the literal strings "Definition", "Definition Source"), not note text - src/refspec/registry/nasa_thesaurus.py:188 says so, and the real text lives in detached blocks the reader deliberately refuses to join. Those claims are already compared by check_reification_fidelity. Remove zthes:termNote from the annotation-literal family so check_annotation_fidelity stops reporting 9,656 false failures. Verify with a scoped run against the real distribution and report the before/after annotation-fidelity numbers for nasa-thesaurus-skos.

TASK 2 - THE LANGUAGE-SCOPE DECLARATION (the owner's decision, made 2026-08-13).
Atlas's product scope is English content; publisher content in other languages is deliberately not carried. That is 97.68% of all audit differences (868,444 of 889,115), currently reported as if it were loss. Make it an EXPLICIT, CHECKED product choice.

The producer side already landed on branch research/fidelity-langbugs (worktree .claude/worktrees/codex-fidelity-langbugs/, commit b079de13): one BCP 47 predicate selects the English family and the wire normalizes to exact @en. THAT BRANCH SPECIFIES YOUR WORK: read its REPORT2.md section on the auditor addition and its exact count payload REPORT2-language-exclusions.json (schemaVersion, exclusionType, selection, predicateFamilies, countsBySourceAndLanguage, totalExcludedClaims). Implement what it specifies.

The constraint that makes this honest, and it is the whole point: the existing DeclaredClaimExclusion mechanism excludes ENTITY LAYERS (whole subject sets Atlas represents nothing about) and REFUSES to exclude predicates on subjects the comparison already compares - deliberately, so it can never become a waiver. Language scope needs something narrower and differently shaped: excluding non-English-tagged literal claims on concepts that ARE compared. Design that as a SEPARATE, fail-closed mechanism - do not weaken DeclaredClaimExclusion. Required properties, all of which must be tested:
  a. It excludes ONLY literal claims carrying a language tag outside the English family - never untagged literals, never IRI claims, never a whole subject.
  b. It fails closed if Atlas asserts ANY non-English-tagged literal anywhere in the distribution (that would mean the scope statement is false - prove it by construction, the way the existing mechanism proves atlasClaimCount == 0).
  c. It is itemised in the receipt exactly like the existing exclusions: per source, per language, per predicate family, with counts - never a silent subtraction. The receipt must let a reader reconstruct exactly what was set aside.
  d. It changes no verdict for English content: every English claim stays compared in both directions.

VERIFY: scoped runs (--only <spec>) against /Users/mikewolfd/Work/spicy-regs/RefSpec/output/atlas-3.1-full-2026-08-13 with sources at /Users/mikewolfd/Work/spicy-regs/RefSpec/output/registry-real-data-sources, for the eight vocabulary sources; report per-source before/after uncovered and differing counts. Then ONE full audit run for the headline (it takes ~11 minutes; check free memory first - other agents share this 48 GB machine). Report the new totals: uncovered claims, per-check pass/fail, and how many of the 889,115 differences are now declared rather than reported as loss.

GATES: make lint; uv run --no-sync pytest tests/test_verify_atlas_source_fidelity.py -q (211 tests today - add yours); the scoped and full audit outputs. Commit incrementally. REPORT.md: both tasks with before/after numbers, the new mechanism's design and its four proven properties, what remains genuinely reported as loss after the declaration, and what a main-tree merge needs.
