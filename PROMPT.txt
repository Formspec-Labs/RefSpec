You are a correctness engineer in a DISPOSABLE git worktree of an RDF vocabulary pipeline. Deliverable: the language-tag bug-class fix, implemented + measured, committed to branch research/fidelity-langbugs, plus REPORT.md. Do NOT touch the main repo at /Users/mikewolfd/Work/spicy-regs/RefSpec outside read-only references to its output/ artifacts.

THE BUG (measured by the fidelity audit): GEMET's adapter tests language tags with an exact match ('en'), so BCP-47 variants (en-US, en-GB) are treated as foreign languages and dropped. Measured real loss: 244 American-spelling synonym labels (sulphide/sulfide, licence/license, organisation/organization class) that matter for search recall; plus ~5,000 en-US labels that are byte-identical to their en twins (no loss, but evidence of the pattern). The wire rule (post-hygiene): language tags are lowercase, and the canonical profile enforces @[a-z]+(-[a-z0-9]+)* - so en-us is representable on the wire.

TASKS:
1. SURVEY the whole bug class: grep every adapter under src/refspec/registry/ and the builder's language handling (tools/generate_atlas_v3_full.py - the English-only selection logic, _audit_english_language_content, and any == 'en' / startswith('en') tests). Build the table: adapter x language-test-style x what gets dropped. Check publisher sources (read-only at /Users/mikewolfd/Work/spicy-regs/RefSpec/output/registry-real-data-sources/) for which actually ship BCP-47 variants (en-US, en-GB, en-CA...) and count affected labels per source.
2. DESIGN the fix once: a single English-family predicate (BCP-47 primary-subtag match: tag == 'en' or tag.startswith('en-')) in ONE shared location the adapters import (check where shared language helpers live - vocabulary.py had require_language_tag; the builder may have its own). Decide and state the wire form: keep the variant tag (en-us per the lowercase rule) or normalize to en? Consider: the seal/canonical profile allows subtags; label identity enters digests (a tag change re-mints label nodes - measure how many); byte-identical duplicate labels (the 5,000) should dedupe rather than emit twins - state the dedupe rule.
3. IMPLEMENT for GEMET first (the measured case), then mechanically for every adapter the survey flags. Rebuild the affected bounded artifacts in the worktree (use --only-release bounded builds, seconds each) and re-run the auditor scoped (--only gemet...) to prove the 244 labels now flow: uv run --no-sync python tools/verify_atlas_source_fidelity.py --distribution <your worktree build> --source-root .../registry-real-data-sources --only <spec>.
4. Tests: one per adapter changed (the label with a variant tag flows; the byte-identical twin dedupes), following the adapter test patterns in tests/.
5. REPORT.md: the survey table, the design decision with its wire/digest consequences (re-mint counts per source), before/after label counts per adapter, auditor verification output, and what the main-tree merge needs (your branch is merged later; the fidelity auditor file is concurrently edited in main - keep your auditor invocations read-only, put ALL changes in adapters/builder/tests).

Peak RSS under 6GB; bounded builds only (never the ~25-min full build).
