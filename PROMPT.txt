You are integrating a proven performance prototype into production, in a git worktree on branch feat/parse-substrate. Deliverable: the integration, its proof, and REPORT.md, committed to this branch. The main repo checkout at /Users/mikewolfd/Work/spicy-regs/RefSpec is READ-ONLY reference (read its output/ artifacts; never write there). Your worktree IS a full RefSpec checkout - do your work here.

THE DECISION ALREADY MADE: take the two-index store + pooled terms substrate. It was researched and measured on branch research/parse-substrate (worktree /Users/mikewolfd/Work/spicy-regs/RefSpec/.claude/worktrees/codex-parse-substrate/, commit e4bce197). READ ITS REPORT.md AND research/parse_substrate/ FIRST - benchmark.py, measurements.json, and the store implementation are all there. Measured at staging (1,013,723 quads): parse+store 20.71s -> 14.78s (-28.6%), peak RSS 1,380 -> 567 MiB (-58.9%); complete semantic path 49.21s -> 33.71s (-31.5%), RSS 1,434 -> 617 MiB (-57.0%); identical verdicts on the real shapes and all 130 corpus cases; OSP index proven never queried by this workload.

WHY IT IS BEING TAKEN - and this governs your priorities: the 57% MEMORY cut matters more than the 31% time cut. Peak RSS at full scale (~20 GB with stock rdflib) is what forces the release workflow's ">=32 GB self-hosted runner" spec (.github/workflows/release.yml). If the ratio holds at 29.3M quads, acceptance fits a standard hosted runner and the deferred infrastructure decision becomes cheap. YOUR PRIMARY DELIVERABLE IS THE FULL-SCALE MEMORY NUMBER.

HARD CONSTRAINTS:
1. The binding validator (bindings/atlas/3.1/tools/validate.py) deliberately imports NO RefSpec package code - a consumer copies bindings/atlas/3.1/ and verifies offline with only its requirements.txt. The store MUST therefore live inside the binding directory (a new module in bindings/atlas/3.1/tools/ imported by relative path the way the validator already loads its siblings - check how rdf_canonical.py is imported - or inline). tests/test_atlas_v3_binding.py enforces the independence claim; keep it passing.
2. Identical verdicts, non-negotiable: all 130 corpus cases with unchanged firstIssue codes and shaclComponents, plus the release-tier cross-mode sweep. The corpus IS the differential battery AGENTS.md requires for a replacement of this kind; run it in both validation modes.
3. pySHACL compatibility is a correctness surface, not a hope: the store must satisfy every Graph/Store API pySHACL 0.31 touches. The research report documents which patterns are queried; verify against the real shapes (bindings/atlas/3.1/shapes/atlas.shacl.ttl), not a subset.
4. Keep a clean seam: the stock rdflib Memory path must remain selectable (an env var or a module-level constant, documented) so a subtle production issue is one flag away from mitigation. State how in REPORT.md.
5. Do NOT touch: tools/verify_atlas_source_fidelity.py, src/refspec/registry/**, src/refspec/atlas/v3_registry_*.py (concurrent agents own those in other worktrees). You own bindings/atlas/3.1/tools/**, its tests, and - only if the memory result justifies it - .github/workflows/release.yml's runner spec.

THE MEASUREMENT (the deliverable):
- Staging first (fast, safe): /Users/mikewolfd/Work/spicy-regs/RefSpec/output/atlas-3.1-mapping-topology-staging/distribution - confirm the research numbers reproduce through the INTEGRATED code path, not the prototype harness.
- Then FULL SCALE: /Users/mikewolfd/Work/spicy-regs/RefSpec/output/atlas-3.1-full-2026-08-13 (29,283,283 quads). Run the complete validate_distribution and record peak RSS + wall time, before and after. CHECK FREE MEMORY FIRST (`vm_stat`): two other codex agents share this 48 GB machine and a stock-rdflib baseline run needs ~20 GB. If free memory is insufficient for the BEFORE run, measure the AFTER run and cite the plan's recorded 20 GB / 75.7-min-era baselines rather than inventing one - and say exactly which numbers are measured vs cited. The AFTER number is the one that matters.
- If peak RSS lands under ~14 GB: update release.yml's runner spec and its comment (the >=32 GB requirement and its rationale), and say plainly in REPORT.md whether a standard hosted runner (16 GB, 14 GB usable) can now host acceptance - including disk for output/ (~7 GB decompressed per distribution) as a separate constraint.

GATES (verbatim in REPORT.md): make lint; make check-generated; make test-atlas-v3; REFSPEC_RELEASE_TIER=1 pytest -k corpus_wide; pytest tests/test_atlas_v3_binding.py tests/test_atlas_v3_validator_regressions.py -q; make contract-dev; pytest -q -n auto; the staging A/B; the full-scale run. Commit incrementally so partial progress survives. REPORT.md: the integration design, the seam, the compatibility evidence, both measurement tables, the runner verdict, and anything you found that the research prototype missed.
