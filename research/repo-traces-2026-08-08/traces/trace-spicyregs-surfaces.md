# Raw trace: SpicyRegs corpora/, tools/, docs/evidence/

Provenance: Sonnet subagent trace, 2026-08-10 (late). HEAD measured: spicy-regs `ac9a25d`
(branch `archive/local-work-2026-08-09`).
Status: **verbatim tracer output, preserved unedited** (transport HTML-escapes fixed). Subagent-produced —
per workspace rule, re-derive before relying on any figure not independently verified; the figures
`../RESEARCH.md` cites from this trace were spot-verified at compile time. OBSERVED / INFERRED /
ASSERTED markers are the tracer's own.

---

## Catalogue: `spicy-regs` research surfaces

**HEAD measured against:** `ac9a25d` (branch `archive/local-work-2026-08-09`). Read-only throughout; no state-changing git commands were run. Pre-existing `M RefSpec` (submodule pointer dirty) in `git status` predates this session and was not touched.

---

## 1. `src/spicy_regs/corpora/`

Total: **23,746 lines** across 15 modules + `__init__.py` (1 line) — OBSERVED, matches expected count exactly (`wc -l`).

Package-wide importer picture (OBSERVED via `grep` for `corpora.<module>` import forms): **exactly 2 files outside `src/spicy_regs/corpora/` import anything from the package** — `src/spicy_regs/document_file_pipeline.py` and `tools/reseal_segmentation_dataset.py`, both importing `segmentation_evaluation.py` specifically. **Both are absent from `origin/main`** (confirmed via `git cat-file -e origin/main:<path>` → fatal: not in origin/main), and `origin/main` has **zero** files under `src/spicy_regs/corpora/` or `tools/` at all — the whole corpora package and tools/ directory are local-only additions on this branch. This matches the stated context exactly (OBSERVED).

- **segmentation_experiment.py** · 3,841 lines · "Run reproducible five-arm segmentation and retrieval experiments." — the shared engine: candidate limits, precision/recall cutoffs, embedding providers, HTML markup parsing. · IMPORTERS: 5 in `src/` (all sibling corpora modules: artifact_retrieval_baseline, segmentation_embedding_audit, segmentation_tagging, segmentation_sparse_retrieval, segmentation_rerank), 9 in `tests/` · No standalone verdict; it's infrastructure other experiments build on. OBSERVED.
- **segmentation_tagging.py** · 2,510 lines · "Compare segmentation arms through the real ontology tagging contract" — uses production prompt, concept registry, exact grounding, checkpoints, provider receipts (not a synthetic proxy). · IMPORTERS: 0 in `src/` (besides its own dependency on segmentation_experiment/document_acceptance_scope), 1 in `tests/` · No verdict recorded in-module. OBSERVED.
- **mixed_real_data.py** · 2,334 lines · "Build and validate a heterogeneous real-data ontology test corpus" — 18 record-bearing tables, ontology-neutral envelope, explicit positive/negative relationship expectations (negatives = "no declared relation in this bound snapshot", not real-world impossibility). · IMPORTERS: 1 in `src/` (segmentation_evaluation.py), 4 in `tests/` · Corresponding evidence: `docs/evidence/mixed-real-data-corpus-2026-07-24/` (CONTAMINATION-WINDOW, receipts only, no verdict prose). OBSERVED.
- **relation_exclusion_evaluation_v2.py** · 2,311 lines · "Fair-v2 contract for target-relation assertion and change-event extraction" — separates immutable source corpus from adjudication oracle; benchmark-eligible comparison blocked until two independent blind human reviews + resolution recorded; does **not** replace v1, only reuses v1's source/target records. · IMPORTERS: 0 in `src/` beyond its own import of v1, 2 in `tests/` · Wired as `validate-relation-exclusion-v2` in pyproject. VERDICT: explicitly none yet — gated open. OBSERVED.
- **segmentation_evaluation.py** · 2,260 lines · "Build a provenance-bound document-segmentation evaluation snapshot" — starts from the balanced 16-profile sample, adds full documents, keeps synthetic failures separate from public evidence. · IMPORTERS: 3 in `src/` (2 sibling corpora modules + `document_file_pipeline.py`), 1 in `tools/` (`reseal_segmentation_dataset.py`), 7 in `tests/` — **the only corpora module with importers outside the package**. VERDICT (from `docs/evidence/document-segmentation-remeasurement-2026-08-02.md`, clean/non-contamination): the committed segmenter now produces 1,296 segments vs. the frozen baseline's 1,302 from **identical code**; cause is environmental (interpreter), not a commit — this is why `pyproject.toml` now pins `requires-python = ">=3.12,<3.13"` with an explicit comment citing this file. OBSERVED.
- **relation_exclusion_evaluation.py** · 1,898 lines · "Locked diagnostic-v1 evaluation with candidate-only OpenAI extraction" — model proposes, a dependency-inverted comparator decides; explicitly states its choices "are unsuitable for a fair model comparison; use a new dataset and schema for v2." · IMPORTERS: 1 in `src/` (v2 imports it), 2 in `tests/` · **Version pair with v2 confirmed**: v1 is frozen/historical ("Diagnostic v1 preserves its original flat modality... so the completed run remains reproducible"), v2 is the live/active successor that projects only v1's content-addressed records forward. OBSERVED.
- **segmentation_rerank.py** · 1,701 lines · "Rerank exact dense candidates with packaged local inference providers." · IMPORTERS: 1 in `src/` (segmentation_sparse_retrieval imports it), 2 in `tests/` · Related literature review: `docs/evidence/arxiv-2403.10407-reranking-review.md` (CONTAMINATION-WINDOW by date — "Adapt; do not use this paper to select a chunker, embedding provider, or ontology identity rule"). OBSERVED.
- **segmentation_sparse_retrieval.py** · 1,522 lines · "Compare learned-sparse and dense+sparse retrieval on fixed segments." · IMPORTERS: 0 in `src/` beyond its own dependency, 2 in `tests/`. OBSERVED.
- **body_retrieval_corpus.py** · 1,496 lines · Extensive docstring: the *old* offline corpus (34 docs, 7 publisher families, median Jaccard ~0.13) made recall@50=1.0 "by arithmetic rather than by merit"; this module draws a new corpus optimized for vocabulary *competition* (one regulatory program — 50 CFR 17, Endangered Species Act) via four receipted stages (draw/fetch/…). · IMPORTERS: 1 in `src/` (mirrulations_document_corpus imports it), 1 in `tests/` · VERDICT (from clean `docs/evidence/body-retrieval-corpus-2026-08-02.md`): built 993 real-body FR documents, median Jaccard rose to **0.2947** (own tokenizer reproduced old ~0.140 as 0.1312); followup doc confirms spicysearch admission is feasible. OBSERVED.
- **artifact_retrieval_baseline.py** · 1,198 lines · "Reproduce whole-artifact retrieval beside segment-level experiments." · IMPORTERS: 0 in `src/` beyond dependencies, 1 in `tests/`. OBSERVED.
- **mirrulations_document_corpus.py** · 900 lines · "Freeze and capture exact Regulations.gov document files from Mirrulations" — draw/fetch/validate/v3-selection over the S3 acquisition source; only `/documents/` records participate, tombstones and superseded revisions excluded. · IMPORTERS: 0 in `src/`, 1 in `tests/` — **one of only 2 corpora modules with zero pyproject entry point.** OBSERVED.
- **document_acceptance_scope.py** · 732 lines · "Build an immutable document-only view over a segmentation dataset" — the most heavily reused internal module (5 sibling corpora files import it). · IMPORTERS: 5 in `src/`, 7 in `tests/`. OBSERVED.
- **segmentation_embedding_audit.py** · 478 lines · "Attach model-native token-limit evidence to a segmentation experiment." · IMPORTERS: 0 in `src/` beyond dependencies, 2 in `tests/`. OBSERVED.
- **profile_evaluation.py** · 359 lines · "Build and validate a bounded all-profile ontology evaluation snapshot." · IMPORTERS: 0 in `src/`, 1 in `tests/`. OBSERVED.
- **embedding_audit.py** · 205 lines · "Model-native embedding input audit contracts and package adapters" — defines `EMBEDDING_INPUT_AUDIT_POLICY_VERSION = "model-native-input-audit-v1"`. · IMPORTERS: 1 in `src/` (segmentation_embedding_audit imports it), 1 in `tests/` — **the other module with zero pyproject entry point.** OBSERVED.
- **`__init__.py`** · 1 line · `"""Reproducible, evidence-bearing test corpora."""` — package docstring only.

**Wiring note (INFERRED from pyproject.toml, OBSERVED text):** 13 of the 15 substantive modules have a dedicated `[project.scripts]` entry (e.g. `build-mixed-real-data-corpus`, `run-relation-exclusion-evaluation`, `validate-relation-exclusion-v2`). Only **`embedding_audit.py`** and **`mirrulations_document_corpus.py`** lack one — both are pure-library modules consumed by sibling corpora files rather than run standalone.

---

## 2. `tools/`

**26 scripts found** (OBSERVED, `find tools -maxdepth 1 -name "*.py" | wc -l` = 26 — matches "~26 expected" exactly). **None** appear in `pyproject.toml [project.scripts]` and none are imported by production `src/` code — so **WIRED = 0/26** (OBSERVED). The "~25 claimed one-off" premise is significantly off, however: **20 of the 26 are dynamically loaded and exercised by dedicated `tests/` files** via `importlib.util.spec_from_file_location` (or direct `sys.path` import), i.e., they have real hermetic test coverage even though they're not production-wired. Only **6 of 26** have zero test-suite coupling. See Anomalies.

TEST-SUPPORT (20 — mapped 1:1 or N:1 to specific test files, OBSERVED via `grep`/direct inspection):

- **ablate_candidate_selectors.py** (3,382 ln) · Ablates 8 candidate-channel compositions against the frozen 35-item dev set; "nothing here adopts anything." · `tests/test_ablate_candidate_selectors.py`.
- **audit_concept_embedding_space.py** (426 ln) · Measures concept-embedding geometry (concept-pair cosine, cross-type null, centroid norm, effective dimensionality, top-1 margin) — see Anomalies (fabrication/correction). · `tests/test_audit_concept_embedding_space.py`.
- **build_agency_crosswalk_artifact.py** (953 ln) · Builds agency-code↔FR-slug crosswalk + CFR-part→agency artifact. · `tests/test_build_agency_crosswalk_artifact.py`.
- **build_date_event_artifact.py** (683 ln) · Unifies comment-period/FR/FCC deadline events into one typed date-event stream. · `tests/test_build_date_event_artifact.py`.
- **build_gold_adjudication_input.py** (506 ln) · Builds the blind judge-input half of the gold-adjudication protocol. · `tests/test_build_gold_adjudication_input.py`, `tests/test_ontology_anchored_selector.py`.
- **build_search_holdout_exam_release.py** (428 ln) · Builds the sealed search-holdout exam `DocumentRelease` over the 240 drawn matters. · `tests/test_build_search_holdout_exam_release.py`.
- **build_usc_act_index_artifact.py** (375 ln) · Builds the pinned U.S. Code popular-names/Table III act index. · `tests/test_build_usc_act_index_artifact.py`.
- **build_usc_source_credit_artifact.py** (283 ln) · Builds the second, independent U.S. Code source-credit index from OLRC USLM XML. · `tests/test_build_usc_source_credit_artifact.py`.
- **discovery_question_cfr60.py** (415 ln) · Discovery question 1 (every docket touching 40 CFR 60), independently re-derived. · loaded by `tests/test_discovery_slice.py`.
- **discovery_question_usc7401.py** (434 ln) · Discovery question 2 (active rulemakings under 42 U.S.C. 7401). · loaded by `tests/test_discovery_slice.py`.
- **discovery_scoring.py** (246 ln) · Shared link/filter/aggregate scoring primitives for the discovery-slice experiments. · loaded by `tests/test_discovery_slice.py`.
- **draft_holdout_gold.py** (824 ln) · Drafts blind, cross-family free-form gold labels for the sealed holdout. · `tests/test_draft_holdout_gold.py`.
- **draw_holdout.py** (1,192 ln) · Draws the untouched Rulespec concept-tagging holdout, seeded/stratified, with `assert_blind` proofs. · `tests/test_draw_holdout.py`.
- **draw_search_holdout.py** (950 ln) · Draws the content-blind SEARCH holdout at matter granularity, reimplementing draw_holdout's protocol. · `tests/test_draw_search_holdout.py`.
- **extraction_bakeoff_worker.py** (490 ln) · Out-of-process, per-candidate text-extraction measurement (anchor reversibility, deletion, FR markers, structure). · `tests/test_extraction_bakeoff.py`; also AST-scanned by `tests/test_docpipeline_adapter_docling.py`'s architecture-boundary allowlist.
- **fuse_concept_registries.py** (1,302 ln) · Migration-only reader reconstructing the historical flat fused-vocabulary artifact; explicitly non-authoritative for production. · `tests/test_fuse_concept_registries.py`, `tests/test_enrichment_reference_runtime.py`.
- **measure_act_relative_resolution.py** (199 ln) · Re-derives act-relative citation resolution numbers through the real loaders (not the discovering script). · `tests/test_measure_act_relative_resolution.py`.
- **project_document_to_rkaf.py** (699 ln) · Projects one corpus document into a gate-valid Rulespec (RKAF) JSON-LD object. · `tests/test_docpipeline_rkaf_projection.py`, `tests/test_project_document_to_rkaf_cli.py`.
- **reseal_segmentation_dataset.py** (236 ln) · Re-seals a segmentation dataset whose identity broke after an in-place table rebuild (commit `3a472f0`); copies, never repairs. · `tests/test_reseal_segmentation_dataset.py`. **Also the sole `tools/` importer of `corpora/segmentation_evaluation.py`.**
- **run_citation_bakeoff.py** (1,207 ln) · Drives the citation-parsing bakeoff (current parser vs. +CiteURL) in 3 phases (detect/adjudicate/verdict). · `tests/test_run_citation_bakeoff.py` (46 tests).

ONE-OFF (6 — no pyproject entry, no import by `src/`, no test coupling found, OBSERVED via exhaustive `grep` across `tests/`):

- **benchmark_usearch_index.py** (979 ln) · Benchmarks a USearch ANN index against exact brute-force dense concept search (build time, latency, RSS, recall@50/12, 8-target oracle). Self-invokes itself via `subprocess` for isolated RSS probing (not a sibling-tool wiring).
- **citation_bakeoff_citeurl_worker.py** (73 ln) · CiteURL detection arm, run in a scratch venv by `run_citation_bakeoff.py`; never imports `spicy_regs`.
- **generate_source_profile_artifacts.py** (70 ln) · Check/write CLI regenerating two deterministic source-profile policy JSON files; not referenced by CI or pre-commit despite its determinism-gate framing.
- **measure_extraction_retention.py** (156 ln) · Measures visible-text retention fraction per corpus/format to derive the extraction coverage floor empirically.
- **prototype_hyperbolic_subsumption.py** (1,068 ln) · Zero-shot HiT-lineage hyperbolic subsumption scorer prototype vs. the gold-adjudication oracle; carries its **own embedded `def test_*` functions**, runnable via `pytest tools/prototype_hyperbolic_subsumption.py` directly, but not collected by the default suite (`testpaths = ["tests"]`).
- **run_extraction_bakeoff.py** (137 ln) · Drives the extraction bakeoff, running each candidate twice in separate processes to catch `PYTHONHASHSEED`-dependent nondeterminism. Only referenced (docstring mention, not loaded) by `tests/test_docpipeline_adapter_docling.py`.

---

## 3. `docs/evidence/` — thematic index

**202 tracked files, 25 dated subdirectories** (OBSERVED, `git ls-files | wc -l` = 202; `find -maxdepth 1 -type d` = 26 incl. `docs/evidence` itself → 25 subdirs — matches expected count exactly). Filesystem mtimes are uniformly `Aug 4 04:27` (a bulk checkout artifact, not real chronology) — dates below are the directories'/files' own embedded `YYYY-MM-DD` names, which is the only reliable dating signal.

### CONTAMINATION-WINDOW clusters (dated on or before 2026-07-28)

- **relation-exclusion bakeoff** (10 dirs, 2026-07-24 to 07-25): `relation-exclusion-openai-probe-2026-07-24`, `relation-exclusion-openai-candidate-run-2026-07-24-02`, `relation-exclusion-codex-cli-{baseline-r2,candidate-run,evidence-first-r1,proof-certificate-r1,proof-certificate-described-r1,semi-formal-r1}-2026-07-25`, `relation-exclusion-openai-v2-focused-five-2026-07-25` (+ `-proof-certificate` variant). Plus standalone `recent-document-relation-lookup-research-2026-07-25.md`, `relation-assertion-adversarial-review-2026-07-25.md`. **TYPE: raw provider-call captures (request/response/receipt JSON) — no README/summary in any of these dirs**, so no verdict was read (OBSERVED structure only, not read for content — respecting the 15-file budget and because no synthesizing file exists). Connects to `corpora/relation_exclusion_evaluation.py` (v1, locked) and `_v2.py` (live successor). CONTAMINATION-WINDOW.
- **Candidate-selection / selector ablation** (2026-07-27/07-28): `candidate-selection-research-2026-07-27.md` (blind external web research, no repo access), `candidate-selector-ablation-2026-07-28/` (README VERDICT: dense channel *reorders*, doesn't find more; quota "buys scheme balance… free at four channels, costs `judicial power` at two"; **development-only, no adoption**). Connects to `tools/ablate_candidate_selectors.py`. CONTAMINATION-WINDOW.
- **Gold-adjudication / failure-analysis / hierarchy-embedding** (2026-07-27/07-28), the methodological core: **`failure-analysis-2026-07-27.md`** — a 6-layer root-cause DECISION RECORD (vocabulary absence, selector silent operating-range failures, RRF fusion tradeoffs, gold semantic failures, **measurement-process contamination** — "information flows downhill from gold into every artifact it touches… Contamination is the default state of a fast iteration loop" — and the tagger-instrument-not-model-quality finding). This is the direct methodological precursor to the executable evaluation-boundary/holdout machinery now in `tools/draw_holdout.py` and `draft_holdout_gold.py`, both of which cite its layers 4/5 by name. `gold-adjudication-2026-07-27/` (README VERDICT: 5/35 adequate registered targets, unanimous; round-2 root-cause correction traced the `scheme`-column collision that later became v2's facet/source_vocabulary split). `hierarchy-embedding-research-2026-07-27.md` (blind external survey). `hyperbolic-subsumption-prototype-2026-07-28.md` (VERDICT: **FAILS** — three pretrained HiT checkpoints land below both the adoption gate and a constant predictor; bounded fine-tune step skipped by design). Connects to `tools/build_gold_adjudication_input.py`, `prototype_hyperbolic_subsumption.py`. CONTAMINATION-WINDOW.
- **USearch / concept-embedding-space** (2026-07-28): **`usearch-ann-benchmark-2026-07-28.md`** — this is the document that literally **contains commit `d165350`'s incident**: an in-place, struck-through "CORRECTION (2026-07-28, after external review)" retracting a "near-degenerate concept space" conclusion whose 0.029 margin figure "compared two different scales" and cited **fabricated** KenMeSH/isotropy/zero-shot-recall figures with no verified source; corrected margin is **+0.2173**. Final (post-correction) VERDICT stands: **REJECT USearch ANN as a swap today** — every memory-saving config loses 21–74% of exact top-12 recall; nothing adopted, `candidate_channels.py` untouched. Connects to `tools/audit_concept_embedding_space.py`, `tools/benchmark_usearch_index.py`. CONTAMINATION-WINDOW **and the literal fabrication site** — but its final numbers are the corrected, externally-reviewed ones, not the retracted ones.
- **Discovery-slice** (`discovery-slice-2026-07-28/` + `.md`): VERDICT question 1 **PASS** (exact set/filter/aggregate match); question 2 originally **FAIL on recall (0.8125)**, then a 2026-08-01 addendum records the fix (`0378a9a`) re-run against the same pinned bytes reaching **1.000** recall — a self-correcting document, similar in shape to the usearch case. Connects to `tools/discovery_question_cfr60.py`, `discovery_question_usc7401.py`, `discovery_scoring.py`. CONTAMINATION-WINDOW by directory date.
- **Segmentation (early)**: `document-segmentation-fair-comparison-2026-07-24.md`, `document-segmentation-v3-step4-local-parity-2026-07-26/` (receipt only), `testbed-execution-2026-07-26.md` (self-labeled "Historical"). CONTAMINATION-WINDOW.
- **Graph-engine bakeoff** (`graph-engine-bakeoff-2026-07-24/`): standalone DuckDB-vs-DuckPGQ-vs-Kuzu spike (own scripts + `results.txt`), self-flagged in its own README as **"Historical… rerun before reporting current real-graph counts."** VERDICT: plain DuckDB stays interactive to 1M edges with no new infra; DuckPGQ variable-length paths are broken on this build; Kuzu has flat ~2ms latency but a per-generation build cost and a second engine to sync. Feeds an external decision doc (`docs/superpowers/specs/2026-07-24-graph-engine-carrier-decision.md`, outside `docs/evidence/`). Not connected to the corpora/tools surfaces. CONTAMINATION-WINDOW.
- **Mixed-real-data-corpus + RIN-ontology-revision** (2026-07-24): both are receipt-only dirs (no README/verdict prose) — pinned BUILD INPUTS for `corpora/mixed_real_data.py`. CONTAMINATION-WINDOW.
- **Rulespec-stabilization** (`rulespec-stabilization-2026-07-24/`): `paired-receipt.json` + 8 `paired-receipt-logs/*.log` — a baseline-vs-candidate gate comparison (compile/test/lint pass/fail per side) for the RuleSpec repo. No narrative README — pure BUILD INPUT / regression-proof record. CONTAMINATION-WINDOW.
- **Single-document Rulespec (RKAF) projection** (`single-document-rulespec-projection-2026-07-28/`): the most rigorously self-verifying doc read — a hand-authored real FR document (2026-03227) projected end-to-end into RKAF JSON-LD, `build_projection.py` hard-fails on any digest/offset mismatch, L1+L2+L3 gates all PASS, 4/5 negative controls correctly fire. VERDICT: **"Realistic — with one operational precondition"** (the compiled JSON-Schema/SHACL context must be pinned and re-vendored with the contract commit). Connects to `tools/project_document_to_rkaf.py`. CONTAMINATION-WINDOW by date, though its own reproducibility machinery is a mitigating factor the task's blanket flag doesn't capture.
- **arxiv-2403.10407-reranking-review.md** (reviewed 2026-07-24): literature review, decision "Adapt; do not use this paper to select a chunker/embedding provider/identity rule." Connects to `corpora/segmentation_rerank.py`. CONTAMINATION-WINDOW.

### Clean clusters (dated after 2026-07-28)

- **Managed-vocabulary-development** (`-2026-07-29/`) and **ELSST R6 forward development dataset** (`-2026-07-29/`): both explicit successor development-target sets, both self-labeled `developmentOnly`/provisional, explicitly do *not* reuse the old fused-registry verdicts. DEVELOPMENT/BUILD INPUT records, not experiment reports.
- **Concept-domain-bridge development** (`-2026-07-30/`, README only): ICPSR→Federal-Register one-hop concept bridge. VERDICT: improved all 7 selected lookup cases (existing selector 6/7 at rank-12, connected selector 7/7 all rank-1); explicitly "selected development probes, not a sealed… evaluation." Not connected to corpora/tools.
- **Body-retrieval-corpus + followup** (`-2026-08-02/` ×2): EXPERIMENT REPORT. VERDICT: replaced the 34-doc/0.140-Jaccard corpus with 993 real-body FR documents at 0.2947 median Jaccard; spicysearch admission confirmed feasible (Part 1 of followup). Connects to `corpora/body_retrieval_corpus.py`, `mirrulations_document_corpus.py`.
- **Citation / USC / agency / date-event / search-holdout cluster** (2026-08-01/08-02, 6 items: `citation-bakeoff-2026-08-02.md`, `agency-crosswalk-2026-08-02.md`, `date-event-artifact-2026-08-01.md`, `search-holdout-draw-2026-08-01.md`, `search-holdout-exam-corpus-2026-08-01.md`): mix of EXPERIMENT REPORT (citation-bakeoff, VERDICT: **do not wire CiteURL**, extend two owned grammars) and pinned BUILD INPUT records (agency-crosswalk, date-event — "built locally, digest-pinned, unpublished" pending a blocked GitHub publication chain). Connects to `tools/run_citation_bakeoff.py`, `build_agency_crosswalk_artifact.py`, `build_date_event_artifact.py`, `draw_search_holdout.py`, `build_search_holdout_exam_release.py`.
- **Extraction-tooling bakeoff** (`-2026-08-02.md`): EXPERIMENT REPORT. VERDICT: **"keep what we have, everywhere. Adopt nothing for HTML, XML, or PDF"** except Chonkie behind the segmentation interface, conditional on a retrieval reason that doesn't currently exist; pypdf recovers text within 2% of modern alternatives across 18 real documents. This is the direct source for the `pymupdf`/`pypdf` version pins and comments in `pyproject.toml`. Connects to `tools/run_extraction_bakeoff.py`, `extraction_bakeoff_worker.py`, `measure_extraction_retention.py`.
- **Segmentation remeasurement** (`document-segmentation-remeasurement-2026-08-02/` + `.md`): EXPERIMENT REPORT / BUILD-INPUT hybrid. VERDICT: the 1,302→1,296 segment-count divergence is environmental (interpreter), not a code change (a prior attribution to commit `e0af2b9` is explicitly withdrawn in a same-day revision note). Directly justifies the `requires-python` pin in `pyproject.toml`. Connects to `corpora/segmentation_evaluation.py`, `tools/reseal_segmentation_dataset.py`.

---

## Anomalies

1. **"~25 claimed one-off" undercounts the tools' test coverage substantially.** Only 6 of 26 `tools/` scripts have zero coupling to the test suite; 20 are dynamically loaded and exercised by dedicated hermetic `tests/*.py` files via `importlib.util.spec_from_file_location`. None is WIRED (no pyproject entry, no `src/` import), so "one-off" is defensible in the *production-wiring* sense the task's CLASS taxonomy uses — but calling them "claimed one-off" without qualification undersells that most have real, targeted regression tests. Recommend treating "TEST-SUPPORT" as "verified-by-dedicated-test" rather than "untested."

2. **The stated "known fabricated-research incident, commit d165350" is not merely upstream of the evidence — it is textually inside `docs/evidence/usearch-ann-benchmark-2026-07-28.md`.** That file contains its own struck-through retraction of a "near-degenerate concept space" claim (fabricated KenMeSH/isotropy citations, wrong-scale 0.029 margin) with an inline correction to +0.2173, applied the same day (`d165350`, 2026-07-28 11:47am ET) the report itself is dated. `docs/evidence/discovery-slice-2026-07-28.md` shows the same self-correcting shape (a 2026-08-01 addendum reversing a recall-failure finding) though that correction is not tied to the fabrication incident specifically. This means CONTAMINATION-WINDOW files are not uniformly "untrustworthy prose" — some carry their own audited corrections in-place, which a blanket per-directory-date flag would miss.

3. **Several evidence subdirectories have no verdict-bearing file at all** — the 10 `relation-exclusion-*` dirs and `mixed-real-data-corpus-2026-07-24/`, `rin-ontology-revision-2026-07-24/`, `rulespec-stabilization-2026-07-24/` contain only raw JSON/log receipts with no README or summary. Any headline conclusion for these lives outside `docs/evidence/` (most likely `docs/decisions.md`, out of this tracer's scope) — flagged rather than guessed at.

4. **`discovery-slice-2026-07-28/`** contains a file named `question-2-usc-42-7401-rescore-2026-08-01.json` — a later-dated rescore artifact living inside a directory whose name embeds the earlier date. Directory-name dating (as instructed) would mis-date this one file by 4 days; worth knowing if any downstream process globs by directory date alone.

5. **No count anomalies against the stated expectations** — corpora line count (23,746), corpora external-importer count (2, both absent from `origin/main`), tools script count (~26 → exactly 26), and evidence tracked-file count (202) all confirmed OBSERVED exactly as stated in the task's "known context."
